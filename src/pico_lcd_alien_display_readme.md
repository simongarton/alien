Whatever is in `pico_lcd_alien_display.py` should be copied to `pico/main.py` and then copied across to the pico.

I am trying to keep the main `alien_generator.py` untouched.

And you can't rename a file in Thonny on the device ?!
